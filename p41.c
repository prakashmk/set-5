#include <stdio.h>
#include<string.h>
void main()
{    char a[10];
     int count=0,b;
    printf("Enter the string: ");
    scanf("%s",a);
    printf("enter the number:");
    scanf("%d",&b);
    for(count=0;count<=b;count++)
    {
    printf("%s\n",a);
}
}
